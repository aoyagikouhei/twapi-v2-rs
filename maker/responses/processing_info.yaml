---

response:
  type: object
  properties:
    check_after_secs:
      type: integer
    progress_percent:
      type: integer
    state:
      type: enum_single
      value: succeeded, in_progress, pending, failed
