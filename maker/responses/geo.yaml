---

response:
  type: object
  properties:
    type: # option
      type: string
    bbox: # option
      type: array
      items:
        type: float
    contained_within: # option
      type: array
      items:
        type: string
    coordinates: # option
      type: object
      ref: coordinates
    place_id:
      type: string
    properties: # option
      type: json
